# ============================================================
# Tableau Multi-Agent System — requirements.txt
# Python 3.12 | Windows 11
# ============================================================
# IMPORTANT: The LangChain ecosystem (langgraph, langchain*) must be
# solved together by pip. Use >= bounds, never == pins for these packages.
# All other packages use exact pins for reproducibility.
# ============================================================

# --- LangChain ecosystem (let pip solve as a group) ---
langgraph>=1.0.0
langchain>=1.2.0
langchain-core>=1.2.0
langchain-anthropic>=1.3.0
langchain-google-genai>=4.2.0

# --- LLM SDKs ---
anthropic>=0.84.0
google-genai>=1.5.0

# --- Tableau ---
tableauserverclient>=0.33.0
tableau-tools>=6.0.0

# --- XML / Data ---
lxml>=5.3.0
pandas>=2.2.0
openpyxl>=3.1.5
great-expectations>=0.18.0

# --- Snowflake ---
snowflake-connector-python>=3.12.0
snowflake-sqlalchemy>=1.6.0

# --- Web / API ---
requests>=2.32.0
httpx>=0.27.0

# --- CLI / Logging ---
click>=8.1.7
rich>=13.9.0
structlog>=24.4.0
python-dotenv>=1.0.1
tenacity>=9.0.0
pyyaml>=6.0.2

# --- State Store ---
sqlalchemy>=2.0.36

# --- Validation ---
jsonschema>=4.23.0
pydantic>=2.9.0

# --- Utilities ---
jinja2>=3.1.4
python-docx>=1.1.2
tabulate>=0.9.0
colorama>=0.4.6

# --- Testing ---
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.24.0
