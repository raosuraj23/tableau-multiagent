# ============================================================
# Tableau Multi-Agent System — requirements.txt
# Python 3.12 | Windows 11
#
# Focused on: Figma → TWB generation + MSTR CSV mapping
# Published Tableau datasources only (no direct DB connections)
# ============================================================

# --- LangChain / LangGraph ecosystem (let pip solve as a group) ---
langgraph>=1.0.0
langchain>=1.2.0
langchain-core>=1.2.0
langchain-anthropic>=1.3.0
langchain-google-genai>=4.2.0

# --- LLM SDKs ---
anthropic>=0.50.0          # Claude (metric mapping + Vision fallback)
google-genai>=1.5.0        # Gemini (optional: documentation agent)

# --- Tableau ---
tableauserverclient>=0.33.0   # Publish workbooks; call Metadata API
tableau-tools>=6.0.0          # TWB/TDS validation helper

# --- XML / Data ---
lxml>=5.3.0                   # XML parsing and validation
pandas>=2.2.0                 # CSV ingestion
openpyxl>=3.1.5               # Excel support (optional input format)

# --- Web / API ---
requests>=2.32.0              # Figma API calls
httpx>=0.27.0                 # Async HTTP (optional)

# --- CLI / Logging ---
click>=8.1.7
rich>=13.9.0
structlog>=24.4.0
python-dotenv>=1.0.1          # Load credentials from .env
tenacity>=9.0.0               # Retry logic

# --- State Store ---
sqlalchemy>=2.0.36            # SQLite ORM for LangGraph checkpointing

# --- Validation ---
jsonschema>=4.23.0
pydantic>=2.9.0

# --- Utilities ---
jinja2>=3.1.4
python-docx>=1.1.2            # Documentation agent output
tabulate>=0.9.0
colorama>=0.4.6
typing-extensions>=4.12.0

# --- Testing ---
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.24.0
